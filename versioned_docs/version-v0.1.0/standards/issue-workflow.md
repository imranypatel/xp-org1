# Issue Workflow Standard

**Version**: v0.1.0  
**Issue**: [#1 ΓÇö Governance: Issue-driven workflow standards](https://github.com/imranypatel/xp-org1/issues/1)  
**Status**: Active

---

## Overview

All work in `xp-org1` is driven by GitHub Issues. No branch, no commit, no document, no plan
is created without a corresponding issue (or comment under one) in the origin repository.

This applies across all domains: Operations, Marketing, Sales, Billing, Procurement, Payments,
HR, CRM, Finance/Accounts, Legal, Production, Project Management, Workflow, IT, Infrastructure,
Architecture, Versioning, Branching, Workspace, DevOps, and Governance.

---

## Issue Lifecycle

```
ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉ
Γöé  1. Issue Created                                               Γöé
Γöé     ΓÇó Title: Domain: Short description                          Γöé
Γöé     ΓÇó Body: verbatim CLI session (if from session) + context   Γöé
Γöé     ΓÇó Labels: domain/X, type/Y, status/in-planning             Γöé
Γöé     ΓÇó Note: for the governance bootstrap issue, labels may be  Γöé
Γöé       applied immediately after the taxonomy is created         Γöé
Γöé     ΓÇó Assigned to: imranypatel (default)                        Γöé
ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö¼ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ
                        Γöé
ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓû╝ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉ
Γöé  2. Agent Assessment (comment on issue)                         Γöé
Γöé     ΓÇó Principal agent assesses domain and risk level            Γöé
Γöé     ΓÇó Recommends expert agents if cross-domain                  Γöé
Γöé     ΓÇó Recommends tools/services                                 Γöé
Γöé     ΓÇó Posts assessment as comment on the issue                  Γöé
ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö¼ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ
                        Γöé
ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓû╝ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉ
Γöé  3. Plan Created                                                Γöé
Γöé     ΓÇó Branch: feature/N-slug (or fix/N-slug, docs/N-slug)      Γöé
Γöé     ΓÇó Plan file: docs/plans/issue-N/{slug}-vX.Y.Z.md           Γöé
Γöé     ΓÇó Plan references the issue number                          Γöé
Γöé     ΓÇó Label updated: status/in-planning                         Γöé
ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö¼ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ
                        Γöé
ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓû╝ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉ
Γöé  4. Work Executed on Branch                                     Γöé
Γöé     ΓÇó All commits on feature/N-slug                             Γöé
Γöé     ΓÇó Label updated: status/in-progress                         Γöé
Γöé     ΓÇó Progress updates posted as comments on the issue          Γöé
ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö¼ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ
                        Γöé
ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓû╝ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉ
Γöé  5. Pull Request Raised                                         Γöé
Γöé     ΓÇó Title: [vX.Y.Z] Short description (#N)                   Γöé
Γöé     ΓÇó Body contains "Closes #N"                                 Γöé
Γöé     ΓÇó Label updated: status/in-review                           Γöé
Γöé     ΓÇó Squash merge preferred                                     Γöé
ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓö¼ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ
                        Γöé
ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓû╝ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉ
Γöé  6. Merge and Tag                                               Γöé
Γöé     ΓÇó PR merged to main                                         Γöé
Γöé     ΓÇó Issue auto-closed by "Closes #N" (GitHub does not apply  Γöé
Γöé       labels on auto-close ΓÇö apply status/done manually)        Γöé
Γöé     ΓÇó Tag: git tag -a vX.Y.Z -m "vX.Y.Z: Description ΓÇö #N"    Γöé
Γöé       git push origin vX.Y.Z    (push the specific tag only)   Γöé
Γöé     ΓÇó Label updated manually: status/done                       Γöé
ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ

### Cancelled / Won't-Fix Path

When an issue is closed without a PR (duplicate, out of scope, decided against):

1. Post a comment on the issue explaining the reason
2. Apply `status/cancelled` or `status/wontfix` label
3. Delete the feature branch if one was created: `git push origin --delete feature/N-slug`
4. If a plan doc was created, add a header note to it: `> **Status: Cancelled** ΓÇö see issue for reason`
5. Close the issue
```

---

## Verbatim CLI Session Capture

When a new issue originates from a CLI session with the principal agent:

1. The full prompt text is included verbatim in the issue body
2. It is clearly identified with the `type/verbatim-cli` label
3. The issue body begins with a standard header:

```markdown
## Verbatim CLI Session ΓÇö Issue #N

> **Source**: Verbatim prompt entered in GitHub Copilot CLI session
> **Date**: YYYY-MM-DD
> **User**: imranypatel
> **Type**: `type/verbatim-cli` (auxiliary) + `type/governance` (primary)
> **Domain**: `domain/X`

---

<verbatim text here>

---

## Agent Assessment
...
```

---

## Issue Title Format

```
Domain: Short description
```

Examples:
- `Governance: Issue-driven workflow standards`
- `Payments: Integrate Stripe billing API`
- `HR: Onboarding workflow automation`

> **Note:** The version is **not** included in the issue title ΓÇö scope often changes between opening and completion, making a version assigned at creation unreliable. The version is assigned at **PR-raise time** and appears in the PR title only (see PR Rules in `branching.md`).

---

## Revision Triggers

An existing issue is revised (new plan version) when:
1. A **new comment** is added to the issue requesting a change in scope or approach
2. A **new linked issue** is created referencing the original

In both cases:
1. A new branch is created (see `branching.md`)
2. A new plan version is created: `{slug}-vX.Y.Z.md` (incrementing from last version)
3. The process restarts from step 3 (Plan Created)

---

## Domain Identification

Every issue must have exactly one `domain/` label. If an issue spans multiple domains,
assign the **primary** domain label and add a comment listing secondary domains.

For cross-domain issues, the principal agent will recommend a team of agents.

See `domain-labels.md` for the full label taxonomy.
